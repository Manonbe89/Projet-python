
class Teleportation : 
    def __init__(self, map) :
        self.current_map = map._get_name_map()  

    def _teleportation(self, player) :
        if self.current_map == "bedroom" :
            if 170 < player._get_pos(0) < 335 :
                map._set_current_map(self.first_village._get_tile())
                player._set_pos(490, 500)
